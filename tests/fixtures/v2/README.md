Fixtures for v2 schema and pipeline tests live next to the Python tests
(`tests/test_v2_schema.py`, `tests/test_pipeline_v2.py`, provider fixtures).
Golden v1 trees remain under `fixtures/discount` and `fixtures/clean_refactor`.
